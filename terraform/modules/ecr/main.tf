resource "aws_ecr_repository" "middleware" {
  name                 = "${var.project_name}-middleware"
  image_tag_mutability = "MUTABLE"

  image_scanning_configuration {
    scan_on_push = true
  }

  tags = {
    Name = "${var.project_name}-middleware"
  }
}

resource "aws_ecr_lifecycle_policy" "middleware" {
  repository = aws_ecr_repository.middleware.name

  policy = jsonencode({
    rules = [
      {
        rulePriority = 1
        description  = "Keep last 5 images"
        selection = {
          tagStatus   = "any"
          countType   = "imageCountMoreThan"
          countNumber = 5
        }
        action = {
          type = "expire"
        }
      }
    ]
  })
}
